# 📘 دليل مشروع "تسديد الحطباني" — تسليم كامل (handoff)

> هذا الملف يوثّق كل ما تم إنجازه بين الجهاز المطوّر والمستخدم حتى تاريخ آخر بناء.
> تقدر تستكمله مع أي مساعد ذكاء اصطناعي (او استكمله بنفسك) — كل التفاصيل موجودة هنا.

---

## 1) ملخص المشروع

تطبيق أندرويد **نقي 100%** (بدون WebView — Kotlin + Jetpack Compose + Material3) لتسديد فواتير الكهرباء في محطة كهرباء الشوافي.

- **الاسم:** تسديد الحطباني — محطة كهرباء الشوافي
- **الحزمة:** `com.shawafi.tasdeed`
- **البناء:** Gradle 8.9 / AGP 8.5.2 / Kotlin 2.0.20 / compileSdk 35 / minSdk 24 / Compose BOM 2024.09.03
- **بيانات Firebase:** REST مباشر عبر OkHttp (بدون Firebase SDK)
  - قاعدة البيانات: `nidaa-meter-app-default-rtdb.asia-southeast1.firebasedatabase.app`
  - الجذر: `v2_nidaa_meter` — مفتاح المزامنة: `NM2026_V2`
  - Firebase apiKey: `AIzaSyDRN-zYxYF3uCviIzIQQwAeRaGflTm6LUY`
  - الأسئلة الشائعة: كل مناطق JSON تعمل عبر `FirebaseClient.kt`

---

## 2) موقع الملفات على هذا الجهاز

```
C:\Users\ComputerWorld\Desktop\محطة كهرباء الشوافي\
├── تطبيق التسديد نيتف\                 ← جذر المشروع (Android Studio)
│   ├── build_apk.py                    ← سكربت الرفع والبناء (أهم أداة)
│   ├── gh_token.txt                    ← توكن GitHub (سري — لا ترفعه)
│   ├── fix_opencode_path.py            ← سكربت نقل المحادثات بين الأجهزة
│   ├── تسديد_نقي.apk                   ← آخر APK جاهز
│   └── app\src\main\java\com\shawafi\tasdeed\
│       ├── MainActivity.kt             ← نقطة الدخول + التنقل
│       ├── ui\AppViewModel.kt          ← كل منطق الحالة
│       ├── ui\theme\Theme.kt           ← الألوان والتدرجات والأشكال
│       ├── ui\screens\                 ← الشاشات الثماني
│       └── data\                       ← Models / LocalStore / FirebaseClient / AppRepository
```

---

## 3) دورة العمل اليومية (طريقة البناء والتسليم)

1. عدّل الكود محلياً في مجلد `تطبيق التسديد نيتف`.
2. شغّل من المجلد:
   ```
   python build_apk.py
   ```
   > في PowerShell استخدم أولاً: `$env:PYTHONIOENCODING='utf-8'` (بدونها ينهار على الرسائل العربية/الإيموجي).
3. السكربت يقوم تلقائياً:
   - زيادة `versionCode` في `app/build.gradle.kts` بمقدار 1
   - مقارنة الملفات المحلية بالملفات المرفوعة في GitHub (أعلى مراجعة) ورفع المتغيّر فقط
   - تشغيل workflow على GitHub Actions (`build-apk.yml`)
   - مراقبة البناء حتى النهاية وتنزيل الـ APK باسم **`تسديد_نقي.apk`** في نفس المجلد
4. سلّم الـ APK للمستخدم.

**خيارات السكربت:**
```
python build_apk.py -skip      ← بناء فقط بدون رفع الملفات
python build_apk.py -monitor   ← مراقبة آخر بناء فقط
python build_apk.py -download  ← تنزيل آخر APK ناجح بدون بناء
```

**مستودع GitHub (خاص):** `774204710773014363mmmm-spec/tasdeed-app`
- التوكن في `gh_token.txt` بجانب السكربت (مستثنى من `.gitignore` فلا يُرفع).
- الـ workflow ملفه: `.github/workflows/build-apk.yml` يبني `assembleDebug` بدون توقيع.

### مشاكل معروفة عند البناء وحلولها
| المشكلة | الحل |
|---|---|
| خطأ `UnicodeEncodeError` عند تشغيل السكربت | `$env:PYTHONIOENCODING='utf-8'` قبل التشغيل |
| انقطاع الشبكة أثناء الرفع (`IncompleteRead`/`WinError 10060`) | الدالة `api()` فيها 4 محاولات إعادة تلقائية مع timeout=60 — أعد تشغيل السكربت فقط |
| رفض رفع ملف كبير خطأ 400 عبر السكربت | رفع يدوي: PUT إلى `/contents/<المسار>` مع `base64` للمحتوى و`sha` للملف الحالي |
| فشل البناء في GitHub | حمّل السجل: `GET /actions/jobs/{job_id}/logs` مع معالج HTMLRedirect يزيل Authorization (المكدس urllib عنده مشكلة 401 على إعادة التوجيه) |

---

## 4) كل الميزات المنفذة (بالترتيب)

### 4.1 الدخول — `LoginScreen.kt`
- لا دخول تلقائي إطلاقاً: شاشة الدخول تظهر دائماً.
- "تذكرني" تحفظ `saved_user` / `saved_pass` وتعبّئ الحقول تلقائياً.
- دخول عبر الشبكة أو **دخول بدون إنترنت** (من ذاكرة التخزين المؤقّتة).
- التحقق: مقارنة username/password مع `branches` في Firebase.
- 🔐 **دخول بالبصمة (جديد v17):** زر ظاهر إذا فعّل المستخدم البصمة في الإعدادات + توجد بيانات محفوظة. يستخدم `BiometricPrompt` (مكتبة `androidx.biometric:1.1.0`)، يسمح بالبصمة القوية أو قفل الشاشة، وعند النجاح يسجّل الدخول بالبيانات المحفوظة.
- التفويضات في الـ Manifest: `USE_BIOMETRIC`، `INTERNET`، `ACCESS_NETWORK_STATE`.

### 4.2 الفواتير — `HomeScreen.kt` (التبويب الأول)
- بحث فوري باسم المشترك / رقم العداد / رقم المشترك.
- بطاقات مشتركين: أول حرفين بأيقونة دائرية، المطلوب بالأحمر إذا > 0 والأخضر إذا = 0.
- **أقفال التعديل:** عند فتح نافذة تسديد تبدأ قفل 60 ثانية على المشترك (أقفال في Firebase `locks`)، جهاز آخر يرى 🔒 وممنوع من الدفع.
  - حلقة الأقفال تجلـب من النت كل **10 ثوانٍ** فقط (لا نضرب الشبكة).
  - العدّاد المتبقي يعتمد على `vm.nowTick` (حلقة فريمات بمعدل `ui_fps` المختار).
- نافذة الدفع: مبلغ (مسبقاً بالمطلوب) + ملاحظة + **اختيار الكشف** (الحالي أو كشف مغلق من الأرشيف).
- `formatNum()` في HomeScreen.kt بمثيل `NumberFormat` واحد مشترك (أداء).

### 4.3 الكشوفات — `ArchiveScreen.kt` + `StatementScreen.kt`
- فتح كشف جديد (يأخذ كل دفعات الكشف الحالي)، إغلاق/جرد، حذف.
- بطاقة كل كشف تعرض: **عدد المشتركين "X مشترك"** (بدل عدد الدفعات) + الإجمالي.
- `StatementScreen.kt` هي **شاشة كاملة** (وليست نافذة):
  - جدول بأعمدة: # / المشترك / العداد / آخر تاريخ / الإجمالي.
  - **ترتيب الجدول والمستندات** بمينو `StatementSort` (الأحدث أولاً / أبجدي / رقم العداد) — يطبّق على الجدول وPDF/Excel معاً.
  - زر تنزيل وزر مشاركة يفتحان **قائمة سفلية (ModalBottomSheet)** من أسفل الشاشة بخيارات: تنزيل PDF، تنزيل Excel، مشاركة PDF، مشاركة Excel.
  - **الإجمالي الكلي شريط مثبّت أسفل الشاشة دائماً** (حتى لو المشتركين قليلين).
  - زر "تعديل المبالغ" (تعديل كامل ثم حفظ) + **ضغطة مطولة على صف مشترك** لتعديل دفعاته فقط في نافذة مركزية.
  - الملفات: PDF/Excel بجودة عربية، تُحفظ عبر نافذة النظام (`CreateDocument`) أو تُشارك عبر `FileProvider` + `ACTION_SEND`.

### 4.4 حساباتي — `FreeScreen.kt` (التبويب الثاني)
- دفعات شخصية **محلية فقط** (SharedPreferences مفتاح `my_account_payments`) — لا تُرفع لـ Firebase.
- زر "➕ تسجيل دفعة": نافذة مركزية (مبلغ + ملاحظة).
- زر "📤 مشاركة / حفظ": قائمة سفلية (مشاركة PDF/Excel أو حفظ PDF/Excel في الجوال).
- جدول (التاريخ/الملاحظة/المبلغ) + **إجمالي مثبّت أسفل الشاشة**.

### 4.5 الإعدادات — `SettingsScreen.kt`
- 🎨 **المظهر:** فاتح/داكن فوري (محفوظ في `theme`).
- 🔤 **حجم الخط:** 80/100/120/140/160% فوري على كل الشاشات (يُطبّق في MainActivity عبر تغيير `density.fontScale`).
- ⚡ **معدل التحديث:** 30/60/90/120 فريم/ثانية.
- 🔐 **بصمة الجوال:** مفتاح تفعيل/إيقاف، يفحص دعم الجهاز (`BiometricManager.canAuthenticate`) وينبّه لو لا توجد بصمة مسجلة.
- 👤 بطاقة المستخدم: الاسم والفرع + زر خروج.
- ⏳ **الدفعات المعلقة:** عرض/حذف/مزامنة (المدفوعات والدفعات الحرة التي لم تصل للشبكة).

### 4.6 الهوية البصرية — `Theme.kt` + `Common.kt` (جديد v17)
- لوحة ألوان زمردية حديثة (primary=#059669، درجات فاتحة/داكنة كاملة مع containers).
- تدرج `GreenBrush` أخضر على: الشريط العلوي بكل الشاشات، شاشة الكشف، زر الدخول.
- شريط سفلي عصري بمؤشر حبّة (pill) للتبويب النشط + خط عريض.
- شاشة الدخول: خلفية متدرجة + بطاقة بيضاء زوايا 24 + حقول دائرية + زر متدرج.
- `Shapes` موحدة (8–28dp) لكل البطاقات والحقول.

---

## 5) بنية الكود الأساسية (ما تحتاج تعرفه للاستمرار)

| الملف | الوظيفة |
|---|---|
| `data/Models.kt` | `PaymentRecord` (فيه `subscriberNumber` المفيد في التقارير)، `Subscriber`، `FreePayment`، `Period` |
| `data/FirebaseClient.kt` | كل اتصالات REST بـ OkHttp (لا Google SDK) — مزامنة، أقفال، فروع الدخول، المشتركين |
| `data/LocalStore.kt` | SharedPreferences + JSON: الأرشيف، الفترات، حساباتي، الإعدادات، بيانات الدخول |
| `data/AppRepository.kt` | يجمع FirebaseClient + LocalStore، جلب/تخزين المخبأ، الدخول |
| `ui/AppViewModel.kt` | كل StateFlows: الدخول، المشتركون، الأقفال، الدفعات، الإعدادات، دعائم البصمة |
| `ui/theme/Theme.kt` | `Green`/`Gray`/`Amber`/`Red` + `GreenBrush` + `TasdeedTheme(dark)` |
| `ui/screens/Common.kt` | `TopBar` (متدرج) + `BottomNavBar` (4 تبويبات: الفواتير/حساباتي/الكشوفات/الإعدادات) |
| `ui/screens/ReportExporter.kt` | `exportPdf/exportExcel(OutputStream, vm, name, list, sort)` — يُكتب مباشرة لتيار الحفظ/المشاركة |
| `MainActivity.kt` | `FragmentActivity` (للبصمة) + حالة الشاشة الحالية + نصّ `Density` لحجم الخط |

### قواعد ذهبية للاستمرار
1. **أي شاشة جديدة تستخدم `Modifier.background` أو `clickable`:** استورد `androidx.compose.foundation.background` و `androidx.compose.foundation.clickable` صراحةً (قرّرها مرتين — سبب فشل بناء سابق).
2. **الحوارات** تُفضّل `Dialog` المركزية؛ **القوائم** (تنزيل/مشاركة/ترتيب) تقلبات سفلية `ModalBottomSheet`.
3. **لا تزيل** شرط "لا دخول تلقائي" — طلب صريح من المستخدم (الضعف الأمني مقصود).
4. أي لمسة ألوان جديدة تستخدم درجات `Theme.kt` فقط لتوحيد الهوية.
5. بعد كل تعديل: `python build_apk.py` فقط (لا نسخ يدوي للـ APK).

---

## 6) آخر إصدار مُسلَّم (أين توقفنا)

- **versionCode 17** — `تسديد_نقي.apk` (~17.6MB) في جذر المشروع.
- آخر ما طلبه المستخدم ونُفذ بالكامل: **بصمة الجوال** + **الهوية البصرية الحديثة**.
- لا توجد مهام معلّقة حالياً — قائمة على طلباتك الجديدة.

---

## 7) نقل المشروع والمحادثات لجهاز آخر

- مجلد النقل الجاهز: `C:\Users\ComputerWorld\Desktop\نقل للابتوب الجديد\`
  - `محطة كهرباء الشوافي\` ← المشروع (يُوضع على سطح المكتب بالجهاز الجديد)
  - `opencode-data\` ← محتوياته تُلصق في `C:\Users\<اسمك>\.local\share\opencode\` (محتواه: `opencode.db` = المحادثات + `auth.json`)
  - `opencode-config\` ← محتوياته في `C:\Users\<اسمك>\.config\opencode\`
  - `المحادثة كاملة.txt` ← نسخة نصية لجميع المحادثات (احتياط مضمون)
  - `OpenCode Desktop Installer.exe` ← مثبّت البرنامج
- إذا اختلف اسم مستخدم الجهاز الجديد عن `ComputerWorld`: شغّل من مجلد `تطبيق التسديد نيتف`:
  ```
  python fix_opencode_path.py "C:/Users/ComputerWorld/Desktop/محطة كهرباء الشوافي" "C:/Users/<اسمك>/Desktop/محطة كهرباء الشوافي"
  ```
- هذا الملف (`دليل المشروع.md`) نسخة منه موجودة أيضاً في مجلد النقل.

---

## 8) جهات الاتصال والمراجع

- المستودع: https://github.com/774204710773014363mmmm-spec/tasdeed-app
- التوكن السري: في `gh_token.txt` (لا ترفعه أبداً، موجود في `.gitignore`)
- ملف الويب القديم `تسديد.html` خارج المجلد الحديث — غير مستخدم في التطبيق الحالي.